using UnityEngine;

[System.Serializable]
public class save_slot
{
    public int player_num;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
